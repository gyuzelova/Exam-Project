export interface User {
    id: string,
    email: string,
    password: string,
    createPost: string[],
    
}

export interface UserAuth {
    id: string,
    email: string,
    password: string,  
}

export interface UserProfil{
    id: string,
    email: string,
    password: string,  
    createPost: string[],
}